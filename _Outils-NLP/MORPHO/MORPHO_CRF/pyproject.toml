[project]
name = "lectura-morpho-crf"
version = "1.0.0"
description = "Analyseur morphologique CRF pour le français (zéro dépendance)"
readme = "README.md"
license = {text = "CC-BY-SA-4.0"}
requires-python = ">=3.10"
dependencies = []

[project.urls]
Homepage = "https://music-music.music"

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
