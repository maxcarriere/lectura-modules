[project]
name = "lectura-liaisons"
version = "1.0.0"
description = "Moteur de liaisons et jonctions pour le français — zéro dépendance"
readme = "README.md"
license = {text = "CC-BY-SA-4.0"}
requires-python = ">=3.10"
dependencies = []

[project.urls]
Homepage = "https://music-music.music"

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"
